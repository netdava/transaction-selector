{
  "name": "transactions-selector-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://netdava.github.io/transaction-selector/",
  "dependencies": {
    "@atlaskit/css-reset": "^5.0.10",
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.5.0",
    "@testing-library/user-event": "^7.2.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1",
    "styled-components": "^5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "replace": "npm run build && rm -rf ./docs && mv build docs",
    "publish-website": "git add docs && git commit -m 'Updated website' && git push origin master"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
